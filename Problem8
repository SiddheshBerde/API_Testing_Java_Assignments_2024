//String a = "Welcome To Java" , count the number of
// space present in the sentense and print only "Welcome" using array.
package Assignments.Problem8;

public class Program8 {

    public static void main(String[] args) {
        String str="Welcome To Java";
        int count=0;

        for (int i = 0; i < str.length(); i++) {
            String check= String.valueOf(str.charAt(i));

            if(check.matches(" "))
            {
                count=count+1;
            }
        }
        System.out.println("White Spaces in string -'Welcome To Java' : " + count);

        //code to get Welcome using Array
        String str1[]=str.split(" ");
        System.out.println(str1[0]);
    }
}
